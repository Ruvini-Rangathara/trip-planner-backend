# ------------------------
# Database & Server
# ------------------------
DATABASE_URL=postgresql://postgres:1234@localhost:5432/ceylon_guide
PORT=9062
JWT_SECRET=u7h2n3k4j5l6m7n8b9v0c1x2z3a4s5d6f7g8h9j0

# ------------------------
# Weather & Forecast (Flask + OpenWeather)
# ------------------------
FLASK_BASE=http://127.0.0.1:8000
FLASK_TIMEOUT_MS=45000      # default 45s; increase if first call still times out
FLASK_RETRIES=2             # number of retries for /forecast, /history
FLASK_RETRY_BACKOFF_MS=800  # base backoff (exponential)

# Suggestion filter thresholds (more forgiving than before)
SUGGEST_BEACH_MAX_RAIN_HI=25
SUGGEST_HIKE_MAX_RAIN_HI=30
SUGGEST_MAX_T_HI=38
SUGGEST_MIN_GOOD_DAYS=1



OPENWEATHER_API_KEY=d33539608647a7ac7afc8f37274ecaa3
OPENWEATHER_BASE=https://api.openweathermap.org/data/2.5
OPENWEATHER_UNITS=metric   # or "imperial"

# ------------------------
# Geocoding (Nominatim)
# ------------------------
NOMINATIM_BASE=https://nominatim.openstreetmap.org
USER_AGENT=trip-planner/1.0 (+mailto:you@example.com)

# ------------------------
# Places (Overpass API)
# ------------------------
OVERPASS_BASE=https://overpass-api.de/api/interpreter
OVERPASS_BASES=https://overpass-api.de/api/interpreter,https://overpass.kumi.systems/api/interpreter,https://overpass.openstreetmap.ru/api/interpreter,https://overpass.osm.ch/api/interpreter,https://maps.mail.ru/osm/tools/overpass/api/interpreter
OVERPASS_TIMEOUT_MS=20000
OVERPASS_RADIUS_MAX=30000
OVERPASS_USER_AGENT=trip-planner/1.0 (+mailto:you@example.com)
